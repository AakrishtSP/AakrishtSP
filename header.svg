<svg viewBox="0 0 400 150" width="100%" xmlns="http://www.w3.org/2000/svg">
  <style>
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.02); }
      100% { transform: scale(1); }
    }
    .title-text {
      animation: pulse 4s ease-in-out infinite;
      font-family: sans-serif;
      font-size: 40px;
      fill: #ffffff;
      font-weight: 500;
    }
    .subtitle-text {
      font-family: sans-serif;
      font-size: 20px;
      fill: #ffffff;
      font-weight: 300;
    }
  </style>
  
  <!-- Transparent background -->
  <rect width="100%" height="100%" fill="transparent" />
  
  <g transform="translate(200 75)">
    <text class="title-text" text-anchor="middle" dominant-baseline="middle">
      Hi, I'm Aakrisht 👋
      <tspan class="subtitle-text" x="0" dy="40" text-anchor="middle">
        Game Engine &amp; Graphics Programming
      </tspan>
    </text>
  </g>
</svg>
